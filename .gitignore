.DS_Store
*.pyc
__pycache__/
.venv/
# Only the top-level scratch directory - the labs commit their measured
# results as evidence, and a bare "results/" would silently swallow those too.
/results/

# Local-only working plan, not part of the published repo
PLAN.md
